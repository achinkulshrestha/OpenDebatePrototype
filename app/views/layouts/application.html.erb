<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "OpenDebate" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Fredericka+the+Great|Josefin+Slab|Slabo+27px' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <!-- Fixed navbar -->
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
            <% if user_signed_in? %>
              <%= link_to('OpenDebate', dashboard_path, {class: "navbar-brand"}) %>
            <% else %>
              <%= link_to('OpenDebate', root_path, {class: "navbar-brand"}) %>
            <% end %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li>
            <% if user_signed_in? %>
              <li>
              <%= link_to('Home', dashboard_path)  %>
              </li>
              <li>
              <%= link_to('Join a Debate', debates_path)  %>
              </li>
              <li>
              <%= link_to('Current & Pending Friends', friendables_path) %>
              </li>
              <li>
              <%= link_to('Find Friends', find_friends_path) %>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Information <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li>
                  <%= link_to('About', about_path)  %>
                  </li>
                  <li>
                  <%= link_to('Help', help_path)  %>
                  </li>
                  <li>
                  <%= link_to('Contact', contact_path)  %>
                  </li>
                  </ul>
              </li>
            <% else %>
              <li>
              <%= link_to('Home', root_path)  %>
              </li>
              <li>
              <%= link_to('About', about_path)  %>
              </li>
              <li>
              <%= link_to('Help', help_path)  %>
              </li>
              <li>
              <%= link_to('Contact', contact_path)  %>
              </li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li>
              <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
              </li>
              <li>
              <%= link_to('Edit Profile', edit_profile_path(id: current_user.id))  %>
              </li>
            <% else %>
              <li>
              <%= link_to('Login', new_user_session_path)  %>
              </li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>

    <div class="container">

          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>

          <%= yield %>

    </div> <!-- /container -->

  </body>
</html>
